name: 'Swm github actions basic'

on: push

env:
  GLOBAL_LEVEL: 'GLOBAL_LEVEL'
jobs:
  first-job:
    name: 'First job'
    stratege:
      matrix:
        os: ['ubuntu-latest', 'windows-latest', 'macos-latest']
    runs-on: ${{ matrix.os }}

    steps:
    - name : say hello word 1 - ${{ matrix.os }}
  
      shell:  bash
      
      run: |
        echo "OS: ${{ matrix.os }}"